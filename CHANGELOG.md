## Changelog

All notable changes to this project are documented here in a high‑level, date‑based format.

### 2026-03-18

- **Desktop mode approval gate**: When "Varsayılan olarak onay iste" is enabled, desktop assistant mode now shows the same approval modal as Cloud LLM before sending the prompt to ChatGPT/Claude. Backend returns `approval_required` with the masked prompt; frontend opens ApprovalModal and on approve resends with `skip_approval: true`. Fixed modal buttons not firing by setting a dummy `sessionId` for desktop approval. Added i18n key for rejection message (EN/TR).
- **Query-time PII sanitization and validation layer**: Chunks remain stored as raw text in the DB (design decision); sanitization runs at query time for both Cloud and Desktop flows with the full pipeline including the Ollama validation layer. Chat router and desktop RAG builder now use PolicyComposer, NER registry, and `enable_ollama_layer=True` at query time so job titles, generic role terms, and organizational locations are filtered as non-PII where appropriate.
- **Ollama PII validation and JSON robustness**: Added language-agnostic `pii_validation_prompt` in PromptCatalog for the validation layer. Made `extract_json_array` in ollama_client tolerant of markdown fences, multiple arrays, trailing commas, and extra text. Sanitizer and NER registry comment/behavior aligned with generic architecture.
- **use_ollama_validation_layer setting**: New backend setting and Privacy UI toggle to enable/disable the Ollama PII validation layer (default true). Backend: config, database seed, AppSettings model, settings router and schema. Frontend: settings page grid, types, i18n (EN/TR).
- **Next.js and lockfile**: Set `outputFileTracingRoot` in next.config.mjs to resolve multi-lockfile warning; removed redundant root `package-lock.json`.

### 2026-03-16

- **Desktop Assistant Mode (ChatGPT / Claude) with RAG support**: Added an optional Desktop Assistant Mode that lets users send their question (or a RAG-enabled prompt with document context) directly to a locally installed desktop assistant client (ChatGPT or Claude) using OS-level automation on the same machine. **RAG integration**: Created `ChatContextPayload` model and `build_chat_prompt` helper to centralize prompt construction logic; refactored Cloud LLM flow to use these abstractions; extended desktop endpoint with `use_rag`, `document_ids`, and `top_k` parameters; when `use_rag=True`, the endpoint retrieves chunks from selected documents, sanitizes query and context using the same pipeline as Cloud Mode, constructs a RAG-enabled prompt via `build_chat_prompt`, and sends the full prompt to the desktop assistant—allowing users to leverage Septum's document context and PII sanitization while using their preferred local LLM client. Frontend: Added "Use document context (RAG)" checkbox in Desktop Mode; when enabled, selected documents are sent to the RAG-enabled desktop endpoint; localized EN/TR i18n keys added. Tests: Added test cases for RAG-enabled and RAG-disabled desktop flows; fixed 7 critical runtime bugs: missing language detector (used existing helper), non-existent DocumentProcessor (used _retrieve_chunks), VectorStore singleton pattern, thread-safe model loading with lock, PyTorch model fallback for Apple Silicon, empty entity filtering, empty chunk filtering. **Critical bug fixes for RAG integration**: Fixed missing import (used existing `_detect_language` helper); replaced non-existent `DocumentProcessor` with `_retrieve_chunks` helper; implemented VectorStore/BM25Retriever singleton pattern to prevent parallel model loading; added thread-safe double-checked locking to fix race conditions; enhanced PyTorch model loading fallback for Apple Silicon meta-tensor errors; added empty entity/chunk filtering in sanitizer; verified end-to-end with 40 chunks and 11.4KB RAG prompt successfully delivered to desktop assistant. Backend: new desktop assistant service layer (`services/desktop_assistant/*`) with platform-specific implementations for macOS (AppleScript + clipboard + keystrokes) and Windows (window activation + keystrokes), plus a dedicated `/api/chat/desktop-assistant/send` endpoint guarded by a `desktop_assistant_enabled` setting so the feature is opt-in and only available on supported platforms. App settings: `AppSettings` and `/api/settings` now include `desktop_assistant_enabled`, `desktop_assistant_default_target`, and `desktop_assistant_chatgpt_new_chat_default`, wired through to the frontend settings response/types. Frontend: Chat page gets a mode switch (Cloud Mode vs Desktop Assistant Mode) and target selector (ChatGPT / Claude); when Desktop mode is active the message is sent via the new endpoint and then into the local desktop client instead of the cloud chat/RAG pipeline, with localized EN/TR status messages. No raw PII beyond the user’s explicit message is logged; desktop automation runs on-device only and does not alter the existing privacy-preserving RAG flow.

### 2026-03-15

- **Audio transcription accuracy and fix**: Improved Whisper-based audio transcription so Turkish (and other languages) produce correct text. Backend now passes the selected Whisper model from settings to the audio ingester (previously always used "base"); optional "default audio language" setting (e.g. Turkish) is passed to Whisper's `transcribe(language=...)`, which significantly improves accuracy. Document `detected_language` for audio now uses Whisper's returned language instead of re-detecting from the transcript text. New setting `default_audio_language` in Ingestion (backend model, migration, API, frontend dropdown with common languages); leave on "Auto-detect" for mixed-language files. **Fix "decoder produced no samples or text"**: temp file for Whisper now uses the correct extension from the upload MIME type (e.g. .mp3, .wav, .ogg) instead of always .m4a, so ffmpeg/Whisper can decode correctly; temp file uses delete=False and is removed in a finally block to avoid platform quirks; audio failure error message now includes ingester warnings for easier debugging. **Fix Apple Silicon (MPS)**: Whisper is forced to run on CPU when the default device would be MPS, avoiding NaN logits and decoder ValueError on Apple Silicon.
- **Changelog maintenance**: Rules updated so changelog documents changes since last push and groups by logical development unit (feature plus related fixes in one bullet). Consolidated same-day entries accordingly.
- **Multi-document chat**: When multiple documents are selected (e.g. three pathology reports), all are now sent to the API and included in context with full detail. Frontend passes all selected IDs (`documentIds`) to ChatWindow and sends `document_ids`; backend prioritizes `document_ids` over single `document_id`. Chunk retrieval: minimum 10 chunks per document in multi-doc mode; full-document threshold raised to 100 chunks so typical multi-report sets get full content. Prompt: "CRITICAL FOR MULTIPLE DOCUMENTS" instructs the LLM to interpret each document individually with full detail and then compare or relate them. Follow-up: removed explicit `JSX.Element` return type on ChatPage to fix TypeScript "Cannot find namespace 'JSX'" with the react-jsx transform.
- **Generic retrieval improvements for holistic queries**: Adaptive top_k, document-theme retrieval with RRF merge, last-chunk inclusion, and holistic-interpretation prompt so broad questions (e.g. "interpret this document") get sufficient context without targeted phrasing. All logic generic; new `merge_rrf_result_lists` and `_effective_top_k`; unit tests in `tests/test_vector_store.py`.

### 2026-03-12

- **Vector search async fix**: Fixed chunk search and chat retrieval timeout issues by wrapping `VectorStore.search()` and `VectorStore.hybrid_search()` (FAISS + ML model operations) with `asyncio.to_thread()` in chunk and chat routers. Blocking synchronous operations (FAISS index search, SentenceTransformer model loading) were freezing FastAPI's event loop for 95+ seconds, causing frontend "Network Error". Now completes in ~3 seconds by offloading to thread pool while keeping event loop responsive.
- **Hybrid retrieval (BM25 + FAISS)**: Implemented professional-grade hybrid search combining keyword-based BM25 with semantic FAISS using Reciprocal Rank Fusion (RRF). Created BM25Retriever service with rank-bm25 library, added hybrid_search method to VectorStore with configurable alpha/beta weights, integrated into document pipeline and chat router. Dramatically improved retrieval quality for contract queries (e.g., "Çalışanın Unvanı") by combining exact term matching with semantic similarity.
- **Table and field extraction**: Added pdfplumber-based structured data extraction for legal/contract documents. Created TableFieldExtractor service to detect tables and key-value pairs from PDFs. Extended Chunk model with chunk_type (clause/field), field_label, field_value, and field_type metadata columns. Integrated into PDF ingester to extract and index structured fields alongside regular text chunks. Key-value pairs (e.g., "Employee Title: Software Engineer") now indexed as separate FieldChunks for better retrieval.
- **Enhanced semantic chunking**: Upgraded StructuredDocumentChunker with LangChain's SemanticChunker using gradient threshold for embedding-based splitting. Hybrid approach: structural phase (numbered sections) + semantic phase (similarity-based splitting). Large sections now split by semantic coherence instead of arbitrary paragraph boundaries. Preserves document structure while respecting semantic flow. Fallback chain: semantic → paragraph → raw.
- **Prompt hardening**: Strengthened chat LLM prompt with anti-hallucination rules: (1) Only answer if exact info in context, (2) If not found, respond with "I cannot find that information in the document", (3) Never invent or extrapolate, (4) Never merge different placeholders, (5) Copy field labels exactly as they appear in context. Prevents fabricated answers for contract queries.
- **Dependencies**: Added rank-bm25==0.2.2, pdfplumber==0.11.5, langchain-text-splitters==1.1.0, langchain-experimental==0.3.6 to requirements.txt.
- **PII detection improvements**: Upgraded all language NER models to state-of-the-art XLM-RoBERTa based models (Davlan/xlm-roberta-base-wikiann-ner for 20 languages, akdeniz27/xlm-roberta-base-turkish-ner for Turkish, Babelscape/wikineural-multilingual-ner for European languages). Improved Ollama Layer 3 prompt to capture exact lowercase person names in context (e.g., family member names). Enhanced blocklist logic to include all entity tokens regardless of casing, ensuring residual mentions are masked. Fixed Ollama span matching to be case-insensitive and increased Ollama output token limit to 2048 for complete JSON responses.
- **KVKK ruleset (6698)**: Expanded entity types to align with Madde 3(d) (kişisel veri: her türlü bilgi) and Madde 6 (özel nitelikli kişisel veriler, 11 kategori). Added: SOCIAL_SECURITY_NUMBER, TAX_ID, COORDINATES, IP_ADDRESS, COOKIE_ID, DEVICE_ID, DNA_PROFILE, MEDICATION, CLINICAL_NOTE, SEXUAL_ORIENTATION, FINANCIAL_ACCOUNT, CREDIT_CARD_NUMBER, BANK_ACCOUNT_NUMBER. REGULATION_ENTITY_SOURCES.md updated with exact article wording, Kurum examples, and full Madde 6 list; recognizer pack docstring updated.
- **Regulation rulesets (GDPR, UK GDPR, CCPA/CPRA, PIPEDA)**: Aligned built-in regulation entity types with official legal texts: GDPR Art. 4(1), Art. 9(1), Recital 30 (added FIRST_NAME, LAST_NAME, ETHNICITY, MAC_ADDRESS, STREET_ADDRESS, CITY, COORDINATES, CLINICAL_NOTE, PASSPORT_NUMBER); UK GDPR same set; CCPA/CPRA § 1798.140 (added ETHNICITY, SOCIAL_SECURITY_NUMBER, FIRST_NAME, LAST_NAME, STREET_ADDRESS, CITY; CPRA added health-related types); PIPEDA s. 2(1) and OPC guidance (added FIRST_NAME, LAST_NAME, STREET_ADDRESS, CITY, MEDICATION). Descriptions and official URLs updated; REGULATION_ENTITY_SOURCES.md updated with exact article/recital wording and PIPEDA subsection.
- **Regulation entity sources doc and rule**: Add backend/docs/REGULATION_ENTITY_SOURCES.md; rule in .cursor requires updating the doc when changing built-in regulation entity types. Seed adds use_ollama_layer to AppSettings defaults.
- **Sanitizer and PII pipeline**: Ollama PII layer wrapped in try/except so failures do not break sanitization; NER uses language-aware confidence threshold (lower for non-English PERSON_NAME/LOCATION); anonymization blocklist adds single-token entities so residual mentions are redacted; NonPiiFilter logic fixed to keep spans when no rule matches; sanitizer_alias_layer prompt made generic (person, location, alias).
- **Chat query sanitization**: User message is now sanitized before retrieval and LLM call (same pipeline as document text).
- **E2E test and ingestion errors**: E2E Turkish PII test no longer skips: uses Presidio-detectable email, language and anon-map mocks, and LLM mock with `on_cloud_failure`; fixture sets `TLDEXTRACT_CACHE` to tmp path. Document upload 500 response now includes `ingestion_error` in detail for easier debugging.
- **NER model overrides**: NER Models settings tab now supports per-language overrides: edit the HuggingFace model ID for any language, restore default per row (persists immediately), and save overrides to the database. Backend stores overrides in `app_settings.ner_model_overrides` and uses them in the sanitizer pipeline and chat.
- **Error logging and Error Log UI**: Centralized error logging for backend and frontend: new `ErrorLog` model and table, `error_logger` service, global exception handler, and `POST /api/error-logs/frontend` for client-reported errors. New Error Logs page under Settings lists, filters, and clears logs with optional stack-trace detail; frontend global error boundary and runtime error listeners report errors to the backend. Sidebar shows error count badge; handled backend errors (e.g. LLM failures, test-llm fallback) are logged; Test LLM returns 200 with `ok: false` when cloud fails and Ollama fallback is used.
- **Document preview**: Copy button shows "Copied" state for user feedback.
- **Chat**: Badge under assistant message when the answer was produced by local Ollama fallback (cloud unavailable).
- **Docs**: README (EN/TR) add Changelog and License links in header.
- **Changelog and rules**: Split changelog entries by commit date (2026-03-11 vs 2026-03-12). Changelog rule updated to require always verifying today’s date (`date +%Y-%m-%d`) and assigning entries to the correct day via `git log --date=short`.
- **Backend dependencies**: Bumped `langchain-experimental` from `0.3.6` to `0.4.1` in `backend/requirements.txt` to restore backend CI compatibility with Python 3.13 on GitHub Actions and keep dependencies aligned with the latest stable version.

- **Recognizer regex and E2E test**: Fix Presidio regex patterns in GDPR, HIPAA, and KVKK packs: use correct word boundary `\b` and escapes so email/phone/IP patterns match. Set TLDEXTRACT_CACHE in conftest before imports to avoid permission errors in E2E tests.
- **Backend tests and CI**: Fix E2E Turkish PII test in CI by preventing PDF OCR from replacing the text layer (patch `PdfIngester._run_ocr_on_page` in fixture so Presidio detects emails). Replace deprecated `datetime.utcnow` with timezone-aware `datetime.now(timezone.utc)` in models (document, regulation, error_log, spreadsheet_schema) and tests. Add `pytest.ini` with filterwarnings to suppress EasyOCR, Presidio, torch, and Swig deprecation warnings in test runs.

### 2026-03-11

- **OCR and PII improvements**: Enhanced image/PDF OCR quality, improved OCR ingestion flow, and refined person name masking and PII handling.
- **Spreadsheet enhancements**: Added spreadsheet schema metadata, numeric-aware chat for tabular content, and limited schema display to truly tabular documents.
- **Infrastructure and tooling cleanup**: Unified environment loading defaults (including Ollama), and removed legacy coverage/Codecov tooling.
- **ODS support**: Added ODS (OpenDocument Spreadsheet) ingestion support and documented it in both English and Turkish READMEs.
- **LLM routing and prompt catalog**: Refactored the LLM router into a provider-strategy layer, introduced a document processing pipeline orchestrator, centralized all backend LLM/Ollama prompts under `PromptCatalog`, and added a shared AppSettings factory plus updated tests.

### 2026-03-10

- **Documentation and licensing**: Expanded README content with PII pipeline and AI gateway sections, screenshot gallery, and clarified extension workflow; added MIT license and kept EN/TR READMEs in sync.
- **Testing and quality**: Improved backend and frontend coverage, added Jest setup, fixed async engine and aiosqlite warnings, and ensured backend tests import the app package correctly.
- **Sanitization and PII pipeline**: Hardened sanitizer structure and robustness, generalized the PII pipeline, added configurable text normalization rules and non‑PII filters, and localized deanonymization banner copy.
- **Chat experience**: Added global i18n for chat UI, approval flow localization, chat debug tools, document‑optional chats, generic prompts, and post‑processing for malformed LLM output.
- **Platform and tooling**: Introduced Dockerfiles and docker‑compose for backend/frontend, tracked env templates, pinned backend dependencies, and updated docs and dependencies.
- **UI and layout**: Refined documents, chunks, and settings UIs; improved sidebar layout; and added the Septum logo across the app.

### 2026-03-09

- **Core platform foundation**: Bootstrapped the Septum project skeleton with core utils, crypto, database models, and health checks.
- **Ingestion pipeline**: Implemented ingestion base and office ingesters (documents, spreadsheets, presentations), plus image and audio ingesters with health checks.
- **Privacy and recognition engine**: Added national ID validators and tests, a multilayer sanitizer, anonymization map with coreference handling, and a regulation‑aware recognizer registry and policy composer.
- **Vector store and retrieval**: Introduced an encrypted FAISS vector store per document and ignored local index artifacts from version control.
- **Backend services and frontend shell**: Added LLM router, deanonymizer, approval gate, chat pipeline wiring, settings sync, settings UI, regulations UI, documents UI, and the initial Next.js frontend shell with layout and API client.
