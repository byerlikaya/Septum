from __future__ import annotations

"""Septum backend package initialization.

Infrastructure configuration is managed by ``bootstrap.py`` which reads
``config.json`` and environment variable overrides. No ``.env`` file is
required.
"""
